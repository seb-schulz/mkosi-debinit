---
# SPDX-License-Identifier: LGPL-2.1-or-later

name: CI
on:
  schedule:
    - cron: "1 3 * * Fri" # utc / 8h00 CEST
  push:
    branches:
      - main
    paths-ignore:
      - "README*"
      - "debian/**"
      - ".devcontainer.json"
  pull_request:
    branches:
      - main
    paths-ignore:
      - "README*"
      - "debian/**"
      - ".devcontainer.json"
jobs:
  tests:
    runs-on: ubuntu-24.04
    concurrency:
      group: ${{ github.workflow }}-${{ toJSON(matrix.distro) }}-${{ matrix.phase }}-${{ github.ref }}
      cancel-in-progress: true
    strategy:
      fail-fast: false
      matrix:
        distro:
          - name: docker.io/debian
            tag: testing
        phase:
          - INITRD_BASIC
          - MKOSI_AND_KVM_CHECK
          - CHECK_DPKG
    container:
      image: "${{ matrix.distro.name }}:${{ matrix.distro.tag }}"
      options: --privileged
      env:
        MKOSI_CACHE: /var/tmp/mkosi-debinit.cache
      volumes: ["/var/tmp:/var/tmp"]
    steps:
      - name: Repository checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - name: Install dependencies
        run: ./scripts/setup.sh
      - name: ${{ matrix.phase }} on ${{ matrix.distro.name }}:${{ matrix.distro.tag }}
        run: ./scripts/tests.sh ${{ matrix.phase }}
  build:
    runs-on: ubuntu-24.04
    container:
      image: "docker.io/debian:testing@sha256:8ac1a6436385cbc6aee56b98a2671c26a26d1f74d6da42d58084c2179355edde"
    steps:
      - name: Repository checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - name: Install dependencies
        run: ./scripts/setup.sh
      - name: Build packages
        run: ./scripts/build.sh
      - name: Upload packages
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: packages
          path: dist/mkosi-debinit*.deb
  lint:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
    container:
      image: "docker.io/debian:testing@sha256:8ac1a6436385cbc6aee56b98a2671c26a26d1f74d6da42d58084c2179355edde"
    needs: build
    steps:
      - name: Repository checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
      - uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: packages
          path: dist/
      - name: Install dependencies
        run: ./scripts/setup.sh
      - name: Check DPKG
        run: ./scripts/lint.sh DPKG
      - name: Check SHELL
        run: ./scripts/lint.sh SHELL
